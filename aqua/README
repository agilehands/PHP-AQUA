Release 0.3.2
------------
1. Added apple locales. easy way to show country flags of supported locale.

However, this applet cannnot be used from Global directory since it uses assets like css and images.
Copy this applet into your application zone. 